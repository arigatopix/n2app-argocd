apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: spo-backend-ing
  namespace: spo
spec:
  rules:
    - host: api-spo.pea.co.th
      $patch: replace
  tls:
    - hosts:
        - api-spo.pea.co.th
