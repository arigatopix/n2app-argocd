apiVersion: v1
kind: Service
metadata:
  name: longhorn-service
  namespace: longhorn-system
spec:
  selector:
    app: longhorn-ui
  ports:
    - protocol: TCP
      port: 80
      targetPort: http
  type: ClusterIP
